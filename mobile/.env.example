# Base URL of the backend REST API (see backend/.env -> PORT + API_PREFIX).
# Android emulator reaches the host machine via 10.0.2.2.
API_BASE_URL=http://10.0.2.2:4000/api/v1

# Socket.io origin (no /api/v1 suffix — it's served from the HTTP root).
SOCKET_URL=http://10.0.2.2:4000
